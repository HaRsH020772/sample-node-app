name: Trigger from docker publish

on:
 push: 
   branches: 
     - "master"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "${{ secrets.NEW_SECRET }}"
      - name: Another message
        run: echo "${{ secrets.NEW_SECRET }}.username"
