name: Trigger from docker publish

on:
 push: 
   branches: 
     - "master"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Message from Trigger from docker publish"
